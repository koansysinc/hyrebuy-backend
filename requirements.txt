# FastAPI and Server
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
mangum==0.17.0

# Database (PostgreSQL via Supabase)
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Pydantic (validation)
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Authentication (without compilation extras)
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.0.1

# Redis (Upstash for caching)
redis==5.0.1

# HTTP Client (for OSRM API)
httpx==0.26.0
aiohttp==3.9.1

# Google Maps
googlemaps==4.10.0

# Email
python-dotenv==1.0.0
